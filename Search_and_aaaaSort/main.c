#include "Functions.h"

#include <stdio.h>
#include <stdlib.h>

#define LENGTH 12

int main()
{
	//int* arr = Rand_Arr_Create(LENGTH);
	//int search_key = 0;

	//printf("Generated Array: ");
	//Arr_Output(arr, LENGTH);

	// ------------------------------- Sort Functions ------------------------------

	//Place_Sort(arr, LENGTH);
	//Shell_Sort(arr, LENGTH);
	//Hibbard_Sort(arr, LENGTH);
	//Pratt_Sort(arr, LENGTH);
	//Bubble_Sort(arr, LENGTH);
	//Cocktail_Sort(arr, LENGTH);
	//Quick_Sort(arr, 0, LENGTH);
	//Comb_Sort(arr, LENGTH);
	//Selection_Sort(arr, LENGTH);
	//Merge_Sort(arr, LENGTH);

	// -----------------------------------------------------------------------------

	//printf("Sorted Array: ");
	//Arr_Output(arr, LENGTH);

	//printf("Enter Search Key\n> ");
	//scanf_s("%d", &search_key);

	// ------------------------------ Search Functions -----------------------------

	//printf("Found Index is: %d", Linear_Search_Unsorted(arr, LENGTH, search_key));
	//printf("Found Index is: %d", Linear_Search_Sorted(arr, search_key));
	//printf("Found Index is: %d", Binary_Search(arr, LENGTH, search_key));
	//printf("Found Index is: %d", Interpolation_Search(arr, LENGTH, search_key));
	//printf("Found Index is: %d", Fibonacci_Search(arr, LENGTH, search_key));

	// ------------------------------- List Functions ------------------------------

	//DEnd_List* list = List_Create();

	//Push_Front(list, 12);
	//Push_Back(list, 4);
	//Push_Front(list, 10);
	//Push_Back(list, 8);
	//Push_Front(list, 1);
	//Push_Back(list, 46);
	//Push_Front(list, 11);

	//printf("List Before Erasing: ");
	//List_Output(list);

	//Pop_Back(list);
	//Pop_Back(list);
	//Pop_Front(list);

	//printf("List After Erasing: ");
	//List_Output(list);

	//List_Erase(list, 12);
	//printf("List After Erasing 12: ");
	//List_Output(list);

	//List_Insert(list, 4, 12);
	//List_Insert(list, 12, 24);

	//printf("List After Iserting 12 After 4 And 24 After 12 : ");
	//List_Output(list);

	// ------------------------------- Hash Functions ------------------------------

	//Hash_Map* map = Set_Hash_Map();

	//Hash_El_Insert(map, 1, 12);
	//Hash_El_Insert(map, 4, 124);
	//Hash_El_Insert(map, 8, 11);
	//Hash_El_Insert(map, 10, 56);
	//Hash_El_Insert(map, 3, 64);
	//Hash_El_Insert(map, 7, 93);
	//Hash_El_Insert(map, 5, 84);

	//printf("Hash Map Before Erasing:\n");
	//Hash_Output(map);

	//Hash_El_Erase(map, 10);
	//Hash_El_Erase(map, 3);
	//Hash_El_Erase(map, 0);

	//printf("Hash Map After Erasing:\n");
	//Hash_Output(map);

	return 0;
}